 وحدة بوابة المعلم

# التكليف 02 - نسيبة عبدالمطلب

## معلومات المهمة
- رمز المهمة: `HEXA AF TEA - 02`
- الساعات المعتمدة: 9 ساعات كحد أقصى
- سعر الساعة: 85 دينار ليبي
- القيمة القصوى: 765 دينار ليبي

## ملخص التنفيذ
تم تنفيذ الجزء الأكاديمي التشغيلي من بوابة المعلم داخل مشروع لارافيل الموحد. تعتمد العمليات على PostgreSQL والعلاقات والصلاحيات الفعلية، ولا تعتمد البيانات الأكاديمية على `localStorage` أو حالة المتصفح.

## Hex_Af ما تم تنفيذه

### 1) المصادقة والترخيص
- تم تسجيل صلاحيات المعلم في `config/permissions.php`:
  - `attendance.manage`
  - `exams.manage`
  - `grades.manage`
  - `homework.manage`
- تم إضافة Middleware `EnsureUserIsTeacher` (alias: `teacher`) في `bootstrap/app.php`.
- تم إضافة مسارات بوابة المعلم في `routes/teacher.php` وتسجيلها داخل `routes/web.php`.

### 2) البنية والبيانات وقاعدة البيانات
- قاعدة البيانات الفعلية في التشغيل المحلي هي PostgreSQL، وتحدد من خلال `.env`:
  - `DB_CONNECTION=pgsql`
  - `DB_DATABASE=afhamha_sis`
  - `DB_HOST=127.0.0.1`
  - `DB_PORT=5432`
- الجداول الأساسية المستخدمة في السيناريو:
  - `users`, `teachers`, `students`, `classrooms`, `subjects`
  - `teacher_assignments`
  - `attendance_records`
  - `exams`, `exam_questions`, `exam_choices`
  - `assignments`, `assignment_attachments`, `assignment_submissions`
  - `grades`, `grade_sheets`
- تم توحيد مخطط الجداول المشتركة بين بوابات المعلم والطالب وولي الأمر. تحتوي جداول الواجبات والمرفقات والأسئلة على أعمدة التوافق اللازمة للنسخ المختلفة، مثل `due_at` و`due_date`، و`question_text` و`text`.
- تم منع تعارض migrations الناتج عن دمج الفروع. تعريفات الجداول الأصلية محفوظة، مع استخدام فحوص `Schema::hasTable()` و`Schema::hasColumn()` عند الحاجة بدل حذف البيانات أو إنشاء جدول مكرر.
- تم جعل بيانات الملفات في `assignment_submissions` اختيارية حتى يمكن حفظ تسليمات المعلم/البيانات الحالية دون ملف، مع بقاء رفع ملفات الطلاب مدعومًا.
- كل عمليات الإنشاء والتعديل والحذف والحفظ تستخدم Eloquent أو `DB` داخل الخادم ومعاملات قاعدة البيانات.

### 3) بوابة المعلم
Controllers الأساسية:
- `app/Http/Controllers/TeacherPortal/DashboardController.php`
- `app/Http/Controllers/TeacherPortal/StudentController.php`
- `app/Http/Controllers/TeacherPortal/AttendanceController.php`
- `app/Http/Controllers/TeacherPortal/ExamController.php`
- `app/Http/Controllers/TeacherPortal/AssignmentController.php`
- `app/Http/Controllers/TeacherPortal/GradeController.php`
- `app/Http/Controllers/TeacherPortal/ProfileController.php`

### 4) نطاق الوصول للمعلم
- تم إنشاء `app/Http/Controllers/TeacherPortal/Concerns/InteractsWithTeacherScope.php`.
- يمنع المعلم من الوصول إلى الصفوف أو المواد أو الطلاب أو الاختبارات أو الواجبات غير المسندة إليه.
- أي محاولة تجاوز تُرجع `403` أو `404` بشكل آمن.

### 5) واجهة المعلم
- تم إنشاء واجهات داخل `resources/views/teacher/`:
  - `dashboard`
  - `students`
  - `attendance`
  - `exams`
  - `assignments`
  - `grades`
  - `profile`
- تم تعديل `resources/views/teacher/layout.blade.php` بحيث:
  - ترتيب القائمة الجانبية يطابق المطلوب
  - اسم المعلم يفتح الملف الشخصي
  - لا تظهر عناصر غير مرتبطة مباشرة بالمعلم في القائمة الجانبية
- تم ضبط أنماط CSS في `public/css/admin.css` و `public/css/teacher-overrides.css` لتتناسب مع شاشة المعلم.

### 6) الاختبارات
- دعم إنشاء اختبار جديد وحفظه كمسودة.
- دعم المتابعة في المسودة من خلال زر "متابعة" في قائمة الاختبارات.
- دعم تعديل الاختبار قبل التاريخ المحدد له.
- إذا تجاوز التاريخ المحدد، يظهر الاختبار كـ "مكتمل" ولا يسمح بالتعديل.
- عند إنشاء اختبار جديد، يظهر زر "حفظ كمسودة".
- عند تعديل اختبار مجدول مستقبليًا، يظهر زر "تحديث الاختبار".
- تم إضافة إمكانية حذف الاختبار مع تأكيد.
- تم تحسين وصف النموذج ووصف الواجبات بشكل أكثر وضوحًا.
- يتم حفظ حقول الاختبار الأساسية والأسئلة والاختيارات والإجابات الصحيحة في `exams` و`exam_questions` و`exam_choices`.
- يتم دعم حالتي `draft` و`scheduled` و`published`، وتظهر الاختبارات المجدولة للطلاب وولي الأمر ضمن الاختبارات القادمة.

### 7) الدرجات
- تم بناء نموذج درجات: كل صف له جدول درجات خاص به.
- تم دعم إضافة عمود جديد، تعديل اسمه، تعديل وزنه، وحذف العمود.
- يتم حساب المتوسط تلقائيًا حسب أوزان الأعمدة.
- لا يتم عرض طلاب غير المسجلين في الصف المختار.
- يتم حفظ بنية الصف في قاعدة البيانات عبر جدول `grade_sheets`.
- قد يتم الاحتفاظ بتفضيلات ترتيب أعمدة شاشة الدرجات مؤقتًا في `localStorage` لتحسين تجربة العرض فقط.
- بنية الصف والأوزان والدرجات الفعلية تحفظ في `grade_sheets` و`grades` داخل قاعدة البيانات، ولا تعتمد على `localStorage`.

### 8) الواجبات
- إنشاء واجبات جديدة.
- تعديل واجب موجود.
- رفع ملف مرفق عند الحاجة.
- متابعة التسليمات وتسجيلها.
- إدخال الدرجات لكل طالب وتسليمها.
- حفظ حالة التسليم والدرجة بشكل منفصل.
- تظهر الواجبات ذات الحالة `published` أو `active` للطلاب وولي الأمر حسب الصف المرتبط.
- يتم ربط الواجبات والطلاب والتسليمات من خلال علاقات قاعدة البيانات، مع منع الوصول إلى صف آخر.

### 9) الحضور
- تسجيل حضور الطلاب حسب الصف والتاريخ.
- حفظ السجلات في `attendance_records`.
- دعم اختيار الصف وتاريخ الحضور مباشرة من شاشة المعلم.

## بيانات الدخول التجريبية بعد التشغيل
- المعلم: `teacher1@example.test` / `password123`
- الطالب: `student1@example.test` / `password123`
- ولي الأمر: `parent@example.test` / `password123`
- المدير: `admin@example.test` / `password123`

هذه حسابات بيانات محلية اختيارية وليست بديلًا عن قاعدة بيانات المشروع الفعلية. لا يقوم `DatabaseSeeder` بإنشاء مستخدم تجريبي أو تشغيل `LocalDemoSeeder` تلقائيًا.

## خطوات التشغيل المحلي
```bash
composer install
cp .env.example .env   # إن لم يكن موجودًا
php artisan key:generate
php artisan migrate --force
php artisan storage:link
php artisan serve
```

لتشغيل بيانات العرض المحلية فقط وبشكل صريح:
```bash
php artisan db:seed --class=Database\\Seeders\\LocalDemoSeeder --force
```
لا تستخدم `migrate:fresh` على قاعدة البيانات الفعلية لأنه يحذف الجداول والبيانات. استخدمه فقط على قاعدة اختبار أو قاعدة محلية مؤقتة.

## السيناريو الكامل للاختبار اليدوي
1. تسجيل الدخول كمعلم → تحويل تلقائي إلى `/teacher/dashboard`.
2. `/teacher/students` → تظهر فقط الطلاب المربوطون بالصف المسند للمعلم.
3. `/teacher/attendance` → اختر التاريخ والصف → سجّل الحضور → يتم حفظ السجلات في `attendance_records`.
4. `/teacher/exams/create` → أنشئ اختبارًا جديدًا، احفظه كمسودة، ثم عد إليه عبر "متابعة" لإضافة الأسئلة أو تعديلها.
5. `/teacher/exams` → لاحظ أن المسودة تظهر بـ "متابعة"، بينما الاختبارات المجدولة المستقبلية تظهر بـ "تعديل"، وبعد تاريخها تظهر "مكتمل".
6. `/teacher/grades` → اختر الصف → أضف/عدّل الأعمدة والأوزان → أدخل الدرجات → احفظ.
7. `/teacher/assignments/create` → أنشئ واجبًا مع وصف، موعد تسليم، ودرجة قصوى.
8. `/teacher/assignments/{id}/submissions` → سجل التسليمات والدرجات لكل طالب.

## معايير القبول والاعتماد
- [x] يعمل السيناريو الكامل: معلم → صف → طالب → حضور → اختبار → درجات.
- [x] يتم حفظ الحضور في `attendance_records`.
- [x] يتم حفظ الاختبارات والأسئلة والاختيارات في `exams` و`exam_questions` و`exam_choices`.
- [x] يتم حفظ الدرجات وبنية سجل الدرجات في `grades` و`grade_sheets`.
- [x] لا يستطيع المعلم تسجيل حضور أو إدخال درجات لطالب خارج الصفوف المسندة إليه.
- [x] لا يستطيع المعلم فتح أو تعديل اختبار يخص معلمًا آخر.
- [x] التحقق من المدخلات يتم في Laravel Form Requests على الخادم، وليس في JavaScript فقط.
- [x] عمليات الواجبات والتسليمات والصلاحيات مرتبطة بقاعدة البيانات الفعلية.
- [x] لا توجد أخطاء حرجة في مسارات التشغيل الأساسية بعد توحيد مخطط قاعدة البيانات.

## التحقق المنفذ
- `php artisan migrate:status`: جميع migrations مطبقة على قاعدة التشغيل.
- `php artisan route:list`: مسارات المعلم والطالب وولي الأمر تسجلت بنجاح.
- تم اختبار الاستجابة المحلية لصفحة الدخول: HTTP `200`.
- اختبارات بوابة الطالب الأكاديمية والمكتبة والتعليم: `79 passed, 0 failed`.
- فحص PHP والتشخيصات للـ controllers المعدلة: دون أخطاء.

## قيود متعمدة
- لا توجد شاشة أداء اختبار من جهة الطالب في هذه المرحلة.
- لا توجد تقارير إدارية شاملة ضمن نطاق هذه الوحدة.
- تعديل الأسئلة في الاختبار يُسمح فقط قبل موعده أو في حالة المسودة، لحماية سلامة النتائج عند النشر.

## خارج نطاق التكليف
- تطوير لوحة المعلم والدروس المكلفة بها المهمة الأولى.
- تنفيذ شاشة أداء الاختبار الخاصة بالطالب.
- إنشاء تقارير إدارية شاملة.
- إعادة تصميم نظام الدخول أو قاعدة البيانات الأساسية دون اعتماد.

## ملاحظات إضافية
- تم تحسين قابلية التمرير داخل الجداول الواسعة لتسهيل القراءة دون الحاجة لتصغير حجم الشاشة.
