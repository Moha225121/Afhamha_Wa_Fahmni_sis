@extends('parent.layout') @section('title','استدعاءات ولي الأمر') @section('content')<section class="page-title"><h1>استدعاءات ولي الأمر</h1></section>@if($children->isNotEmpty()) @include('parent.partials.child-switcher') @endif <section class="list-section">@forelse($calls as $call)<div class="list-row"><div><strong>{{ $call->reason }}</strong><span>{{ $call->guardian_visible_details }}</span><span>{{ $call->meeting_date?->format('Y-m-d H:i') }}</span></div><b>{{ ['pending'=>'قيد الانتظار','notified'=>'تم الإبلاغ','scheduled'=>'تم تحديد موعد','completed'=>'تمت المقابلة','cancelled'=>'ملغي'][$call->status]??$call->status }}</b></div>@empty<p>لا توجد استدعاءات.</p>@endforelse</section>@endsection
