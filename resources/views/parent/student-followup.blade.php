@extends('parent.layout') @section('title','متابعة الطالب') @section('content')<section class="page-title"><h1>متابعة الطالب</h1></section>@if($children->isNotEmpty()) @include('parent.partials.child-switcher') @endif<section class="list-section">@forelse($notes as $note)<div class="list-row"><div><strong>{{ $note->title }}</strong><span>{{ $note->content }}</span></div><b>{{ $note->created_at->format('Y-m-d') }}</b></div>@empty<p>لا توجد ملاحظات متاحة لولي الأمر.</p>@endforelse</section>@endsection
