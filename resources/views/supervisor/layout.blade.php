<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title') | افهمها وفهمني</title><link rel="stylesheet" href="{{ asset('css/admin.css') }}">@include('shared.branding-style')</head><body><button class="menu-toggle" id="menu">☰</button><aside class="sidebar" id="sidebar"><a class="logo" href="{{ route('supervisor.dashboard') }}"><span class="brand-mark">@include('shared.brand-mark')</span><span><b>افهمها وفهمني</b><small>بوابة المشرف</small></span></a><nav>@foreach(['dashboard'=>'الرئيسية','attendance.index'=>'حضور اليوم','attendance.history'=>'سجل الحضور','students.index'=>'الطلاب','guardian-calls.index'=>'استدعاءات أولياء الأمور','student-notes.index'=>'الملاحظات والمتابعة','reports.index'=>'التقارير'] as $route=>$label)<a href="{{ route('supervisor.'.$route) }}" class="{{ request()->routeIs('supervisor.'.$route)?'active':'' }}">{{ $label }}</a>@endforeach</nav></aside><div class="page"><header class="topbar"><div class="account"><span>{{ auth()->user()->name }}</span><form method="post" action="{{ route('logout') }}">@csrf<button>خروج</button></form></div></header><main class="content">@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error"><ul>@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif<div class="page-head"><div><p class="eyebrow">بوابة المشرف</p><h1>@yield('title')</h1></div></div>@yield('content')</main></div><script>document.getElementById('menu').onclick=()=>document.getElementById('sidebar').classList.toggle('open')</script>@stack('scripts')</body></html>
