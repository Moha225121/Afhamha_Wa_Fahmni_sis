@extends('supervisor.layout') @section('title','الطلاب') @section('content')<form class="filters"><input name="q" value="{{ request('q') }}" placeholder="الاسم أو الرقم الدراسي"><button>بحث</button></form><div class="table-wrap"><table><thead><tr><th>الطالب</th><th>الرقم</th><th>الصف والفصل</th></tr></thead><tbody>@foreach($students as $s)<tr><td><a href="{{ route('supervisor.students.show',$s) }}">{{ $s->user->name }}</a></td><td>{{ $s->student_number }}</td><td>{{ $s->classroom?->name }} {{ $s->classroom?->section }}</td></tr>@endforeach</tbody></table></div>{{ $students->links() }}@endsection
