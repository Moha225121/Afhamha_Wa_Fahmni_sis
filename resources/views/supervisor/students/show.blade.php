@extends('supervisor.layout') @section('title','ملف الطالب') @section('content')<section class="card"><h2>{{ $student->user->name }}</h2><p>{{ $student->student_number }} — {{ $student->classroom?->name }} {{ $student->classroom?->section }}</p><p>ولي الأمر: {{ $student->guardians->pluck('user.name')->join('، ') ?: 'غير مرتبط' }}</p></section><div class="grid-2"><section class="card"><h2>ملاحظة متابعة</h2><p class="muted">الملاحظات الصحية والنفسية للمتابعة فقط وليست تشخيصاً طبياً أو نفسياً.</p><form method="post" action="{{ route('supervisor.student-notes.store',$student) }}">@csrf<select name="type"><option value="general">عامة</option><option value="health">صحية</option><option value="psychological">نفسية</option><option value="behavioral">سلوكية</option><option value="administrative">إدارية</option></select><input name="title" required placeholder="العنوان"><textarea name="content" required></textarea><select name="visibility"><option value="internal">داخلية فقط</option><option value="guardian">إظهار لولي الأمر</option></select><label><input type="checkbox" name="requires_guardian_call" value="1"> يحتاج استدعاء ولي الأمر</label><button>حفظ</button></form></section><section class="card"><h2>استدعاء ولي الأمر</h2><form method="post" action="{{ route('supervisor.guardian-calls.store',$student) }}">@csrf<select name="category"><option value="attendance">الحضور</option><option value="repeated_absence">تكرار الغياب</option><option value="repeated_lateness">تكرار التأخير</option><option value="behavior">السلوك</option><option value="health">الصحة</option><option value="psychological">المتابعة النفسية</option><option value="other">أخرى</option></select><input name="reason" required placeholder="السبب"><textarea name="details" placeholder="تفاصيل داخلية"></textarea><textarea name="guardian_visible_details" placeholder="التفاصيل المسموح بعرضها"></textarea><input type="datetime-local" name="meeting_date"><button>إنشاء الاستدعاء</button></form></section></div><section class="card"><h2>آخر الحضور</h2>@foreach($student->attendances as $a)<p>{{ $a->date->format('Y-m-d') }} — {{ $a->status->label() }}</p>@endforeach</section><section class="card"><h2>الملاحظات</h2>@foreach($student->studentNotes as $n)<p>{{ $n->title }} — {{ $n->visibility==='internal'?'داخلية':'لولي الأمر' }}</p>@endforeach</section>@endsection
