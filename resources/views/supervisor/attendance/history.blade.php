@extends('supervisor.layout') @section('title','سجل الحضور والتقارير') @section('content')<form class="filters"><input type="date" name="date_from" value="{{ request('date_from') }}"><input type="date" name="date_to" value="{{ request('date_to') }}"><select name="status"><option value="">كل الحالات</option>@foreach($statuses as $s)<option value="{{ $s->value }}" @selected(request('status')===$s->value)>{{ $s->label() }}</option>@endforeach</select><button>تصفية</button></form><div class="table-wrap"><table><thead><tr><th>التاريخ</th><th>الطالب</th><th>الفصل</th><th>الحالة</th><th>التأخير</th></tr></thead><tbody>@foreach($records as $r)<tr><td>{{ $r->date->format('Y-m-d') }}</td><td>{{ $r->student->user->name }}</td><td>{{ $r->student->classroom->name }}</td><td>{{ $r->status->label() }}</td><td>{{ $r->late_minutes }}</td></tr>@endforeach</tbody></table></div>{{ $records->links() }}@endsection
