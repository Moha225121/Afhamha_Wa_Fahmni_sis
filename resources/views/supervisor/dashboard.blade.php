@extends('supervisor.layout') @section('title','الرئيسية') @section('content')<div class="cards">@foreach(['students'=>'طلاب اليوم','present'=>'الحاضرون','absent'=>'الغائبون','late'=>'المتأخرون','excused'=>'بعذر','calls'=>'استدعاءات مفتوحة'] as $key=>$label)<article class="card stat"><span>{{ $label }}</span><strong>{{ $stats[$key] }}</strong></article>@endforeach</div><div class="grid-2"><section class="card"><h2>الغائبون اليوم</h2>@forelse($absentStudents as $a)<p><a href="{{ route('supervisor.students.show',$a->student) }}">{{ $a->student->user->name }}</a></p>@empty<p>لا يوجد.</p>@endforelse</section><section class="card"><h2>المتأخرون اليوم</h2>@forelse($lateStudents as $a)<p>{{ $a->student->user->name }} — {{ $a->late_minutes }} دقيقة</p>@empty<p>لا يوجد.</p>@endforelse</section></div><section class="card"><h2>آخر ملاحظات المتابعة</h2>@forelse($recentNotes as $n)<p>{{ $n->student->user->name }} — {{ $n->title }}</p>@empty<p>لا توجد.</p>@endforelse</section>@endsection
