@extends('admin.layout') @section('title','المعلمون') @section('actions')<a class="btn primary" href="{{ route('admin.teachers.create') }}">إضافة معلم</a>@endsection @section('content')<form class="filters"><input name="q" value="{{ request('q') }}" placeholder="الاسم أو البريد"><button class="btn secondary">بحث</button></form><div class="table-wrap"><table><thead><tr><th>الاسم</th><th>التخصص</th><th>المواد</th><th>الصفوف</th><th>الهاتف</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($teachers as $t)<tr><td>{{ $t->user->name }}</td><td>{{ $t->specialization ?: '—' }}</td><td>{{ $t->subjects->pluck('name')->unique()->join('، ') ?: '—' }}</td><td>{{ $t->classrooms->pluck('name')->unique()->join('، ') ?: '—' }}</td><td>{{ $t->user->phone ?: '—' }}</td><td><span class="badge {{ $t->status }}">{{ $t->status }}</span></td><td><a href="{{ route('admin.teachers.edit',$t) }}">تعديل</a></td></tr>@empty<tr><td colspan="7"><div class="empty">لا يوجد معلمون حاليًا.</div></td></tr>@endforelse</tbody></table></div>{{ $teachers->links() }}@endsection
