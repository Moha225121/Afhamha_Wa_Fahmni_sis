@extends('admin.layout') @section('title',$class->exists?'تعديل الصف':'إضافة صف') @section('content')<form class="form-card" method="post" action="{{ $class->exists?route('admin.classes.update',$class):route('admin.classes.store') }}">@csrf @if($class->exists)@method('put')@endif<div class="form-grid"><label>اسم الصف<input name="name" value="{{ old('name',$class->name) }}" required></label><label>المرحلة<input name="stage" value="{{ old('stage',$class->stage) }}" required></label><label>الشعبة<input name="section" value="{{ old('section',$class->section) }}"></label><label>السنة الدراسية<select name="academic_year_id" required>@foreach($years as $y)<option value="{{ $y->id }}" @selected(old('academic_year_id',$class->academic_year_id)==$y->id)>{{ $y->name }}</option>@endforeach</select></label><label class="wide">المواد<select multiple name="subject_ids[]">@foreach($subjects as $s)<option value="{{ $s->id }}" @selected(in_array($s->id,old('subject_ids',$class->subjects?->pluck('id')->all()??[])))>{{ $s->name }}</option>@endforeach</select></label></div><div class="form-actions"><button class="btn primary">حفظ</button></div></form>@endsection
