@extends('admin.layout') @section('title','تفاصيل الصف') @section('actions')<a class="btn primary" href="{{ route('admin.classes.edit',$class) }}">تعديل</a>@endsection @section('content')<div class="panel"><h2>{{ $class->name }} {{ $class->section }}</h2><p>{{ $class->stage }} — {{ $class->academicYear->name }}</p><p>المواد: {{ $class->subjects->pluck('name')->join('، ') ?: 'لا توجد' }}</p><p>الطلاب: {{ $class->students->pluck('user.name')->join('، ') ?: 'لا يوجد' }}</p></div>@endsection
