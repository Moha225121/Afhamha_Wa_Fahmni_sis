@extends('admin.layout') @section('title','إنشاء اختبار') @section('content')<form class="form-card" method="post" action="{{ route('admin.exams.store') }}">@csrf<div class="form-grid"><label>العنوان<input name="title" required></label><label>المادة<select name="subject_id">@foreach($subjects as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select></label><label>الصف<select name="classroom_id">@foreach($classrooms as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select></label><label>المعلم<select name="teacher_id">@foreach($teachers as $x)<option value="{{ $x->id }}">{{ $x->user->name }}</option>@endforeach</select></label><label>موعد البدء<input type="datetime-local" name="starts_at" required></label><label>المدة بالدقائق<input type="number" name="duration_minutes" min="1" required></label><label>الدرجة الكلية<input type="number" step=".01" name="total_score" required></label><label>الحالة<select name="status"><option value="draft">مسودة</option><option value="scheduled">مجدول</option><option value="published">منشور</option></select></label></div><div class="form-actions"><button class="btn primary">حفظ</button></div></form>@endsection
