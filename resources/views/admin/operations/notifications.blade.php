@extends('admin.layout') @section('title','الإشعارات') @section('actions')<form method="post" action="{{ route('admin.notifications.read') }}">@csrf @method('patch')<button class="btn secondary">تعليم الكل كمقروء</button></form>@endsection @section('content')<div class="panel">@forelse($rows as $r)<div class="activity"><b>{{ $r->data['title']??'إشعار' }}</b><p>{{ $r->data['message']??'' }}</p><small>{{ $r->created_at->diffForHumans() }} {{ $r->read_at?'':'· جديد' }}</small></div>@empty<div class="empty">لا توجد إشعارات.</div>@endforelse</div>{{ $rows->links() }}@endsection
