@extends('admin.layout') @section('title','المكتبة الرقمية') @section('content')<form class="form-card" enctype="multipart/form-data" method="post" action="{{ route('admin.library.store') }}">@csrf<div class="form-grid"><label>العنوان<input name="title" required></label><label>التصنيف<input name="category"></label><label>المادة<select name="subject_id"><option value="">عام</option>@foreach($subjects as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select></label><label>الصف<select name="classroom_id"><option value="">كل الصفوف</option>@foreach($classrooms as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select></label><label class="wide">الملف (20MB كحد أقصى)<input type="file" name="file" required></label><label><input type="checkbox" name="is_public" value="1"> مورد عام</label></div><button class="btn primary">رفع المورد</button></form><br><div class="table-wrap"><table><thead><tr><th>العنوان</th><th>التصنيف</th><th>عام</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($rows as $r)<tr><td>{{ $r->title }}</td><td>{{ $r->category?:'—' }}</td><td>{{ $r->is_public?'نعم':'لا' }}</td><td>{{ $r->status }}</td><td><form method="post" action="{{ route('admin.library.destroy',$r->id) }}">@csrf @method('delete')<button class="btn secondary" data-confirm="سيُحذف الملف نهائيًا، هل أنت متأكد؟">حذف</button></form></td></tr>@empty<tr><td colspan="5"><div class="empty">لا توجد موارد.</div></td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}@endsection
