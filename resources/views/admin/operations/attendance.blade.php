@extends('admin.layout') @section('title','متابعة الحضور') @section('subtitle','ملخص الحضور حسب الصف') @section('content')<form class="filters" method="get" action="{{ route('admin.attendance.index') }}"><input type="date" name="date" value="{{ $date }}"><button class="btn secondary">عرض</button></form><div class="table-wrap"><table><thead><tr><th>السنة الدراسية</th><th>الصف</th><th>الطلاب</th><th>الحضور</th><th>الغياب</th><th>التأخر</th><th>بعذر</th><th>النسبة</th></tr></thead><tbody>@forelse($attendance as $row)<tr><td>{{ $row->academic_year }}</td><td>{{ $row->classroom }} {{ $row->section }}</td><td>{{ $row->students_total }}</td><td>{{ $row->present }}</td><td>{{ $row->absent }}</td><td>{{ $row->late }}</td><td>{{ $row->excused }}</td><td><span class="badge active">{{ $row->rate }}%</span></td></tr>@empty<tr><td colspan="8"><div class="empty">لا توجد صفوف.</div></td></tr>@endforelse</tbody></table></div>@endsection
