@extends('admin.layout') @section('title','الاختبارات') @section('content')<div class="table-wrap"><table><thead><tr><th>الاختبار</th><th>المادة</th><th>الصف</th><th>المعلم</th><th>التاريخ</th><th>الدرجة</th><th>الحالة</th></tr></thead><tbody>@forelse($rows as $r)<tr><td>{{ $r->title }}</td><td>{{ $r->subject }}</td><td>{{ $r->classroom }}</td><td>{{ $r->teacher }}</td><td>{{ $r->starts_at }}</td><td>{{ $r->total_score }}</td><td><span class="badge status-{{ $r->effective_status ?? $r->status ?? 'draft' }}">{{ match($r->effective_status ?? $r->status ?? 'draft') { 'draft' => 'مسودة', 'scheduled' => 'مجدول', 'published' => 'منشور', 'completed' => 'مكتمل', 'cancelled' => 'ملغى', default => 'غير معروف' } }}</span></td></tr>@empty<tr><td colspan="7"><div class="empty">لا توجد اختبارات.</div></td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}@endsection
