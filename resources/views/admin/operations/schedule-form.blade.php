@extends('admin.layout') @section('title','إضافة حصة') @section('content')<form class="form-card" method="post" action="{{ route('admin.schedules.store') }}">@csrf<div class="form-grid"><label>الصف<select name="classroom_id" required>@foreach($classrooms as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select></label><label>المعلم<select name="teacher_id" required>@foreach($teachers as $x)<option value="{{ $x->id }}">{{ $x->user->name }}</option>@endforeach</select></label><label>المادة<select name="subject_id" required>@foreach($subjects as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select></label><label>اليوم<select name="day_of_week">@foreach(['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس'] as $i=>$d)<option value="{{ $i }}">{{ $d }}</option>@endforeach</select></label><label>البداية<input type="time" name="starts_at" required></label><label>النهاية<input type="time" name="ends_at" required></label><label>القاعة<input name="room"></label></div><div class="form-actions"><button class="btn primary">حفظ</button></div></form>@endsection
