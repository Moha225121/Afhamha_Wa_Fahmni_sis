@extends('admin.layout') @section('title','الجداول الدراسية') @section('actions')<a class="btn primary" href="{{ route('admin.schedules.create') }}">إضافة حصة</a>@endsection @section('content')<form class="filters"><select name="classroom_id"><option value="">كل الصفوف</option>@foreach($classrooms as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select><button class="btn secondary">تصفية</button></form><div class="table-wrap"><table><thead><tr><th>اليوم</th><th>الصف</th><th>المادة</th><th>المعلم</th><th>الوقت</th><th>القاعة</th><th></th></tr></thead><tbody>@forelse($rows as $r)<tr><td>{{ ['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت'][$r->day_of_week] }}</td><td>{{ $r->classroom }}</td><td>{{ $r->subject }}</td><td>{{ $r->teacher }}</td><td>{{ $r->starts_at }}–{{ $r->ends_at }}</td><td>{{ $r->room?:'—' }}</td><td><form method="post" action="{{ route('admin.schedules.destroy',$r->id) }}">@csrf @method('delete')<button class="btn secondary" data-confirm="حذف الحصة؟">حذف</button></form></td></tr>@empty<tr><td colspan="7"><div class="empty">لا توجد حصص مجدولة.</div></td></tr>@endforelse</tbody></table></div>{{ $rows->links() }}@endsection
