@extends('admin.layout') @section('title','الطلاب') @section('actions')<a class="btn primary" href="{{ route('admin.students.create') }}">إضافة طالب</a>@endsection @section('content')<form class="filters"><input name="q" value="{{ request('q') }}" placeholder="الاسم أو الرقم الدراسي"><select name="classroom_id"><option value="">كل الصفوف</option>@foreach($classrooms as $c)<option value="{{ $c->id }}" @selected(request('classroom_id')==$c->id)>{{ $c->name }}</option>@endforeach</select><select name="status"><option value="">كل الحالات</option><option value="active">نشط</option><option value="inactive">غير نشط</option></select><button class="btn secondary">بحث</button></form><div class="table-wrap"><table><thead><tr><th>الاسم</th><th>الرقم الدراسي</th><th>الصف</th><th>ولي الأمر</th><th>الحالة</th><th>الإجراءات</th></tr></thead><tbody>@forelse($students as $s)<tr><td>{{ $s->user->name }}</td><td>{{ $s->student_number }}</td><td>{{ $s->classroom?->name ?? '—' }}</td><td>{{ $s->guardians->pluck('user.name')->join('، ') ?: '—' }}</td><td><span class="badge {{ $s->status }}">{{ $s->status==='active'?'نشط':'موقوف' }}</span></td><td><a href="{{ route('admin.students.show',$s) }}">عرض</a> · <a href="{{ route('admin.students.edit',$s) }}">تعديل</a></td></tr>@empty<tr><td colspan="6"><div class="empty">لا توجد بيانات طلاب حاليًا.<br><a class="btn primary" href="{{ route('admin.students.create') }}">إضافة طالب</a></div></td></tr>@endforelse</tbody></table></div>{{ $students->links() }}@endsection
