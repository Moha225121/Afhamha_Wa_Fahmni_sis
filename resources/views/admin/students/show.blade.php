@extends('admin.layout') @section('title','ملف الطالب') @section('actions')<a class="btn secondary" href="{{ route('finance.students.show',$student) }}">الحالة المالية</a> <a class="btn secondary" href="{{ route('admin.students.account-card',$student) }}" target="_blank" rel="noopener">طباعة بطاقة الحساب</a> <a class="btn primary" href="{{ route('admin.students.edit',$student) }}">تعديل</a>@endsection @section('content')<p><a class="btn secondary" href="{{ route('admin.students.analysis', $student) }}">تحليل مستوى الطالب وخطة الدعم بالذكاء الاصطناعي</a></p><div class="panel"><h2>{{ $student->user->name }}</h2>@php($financial=app(\App\Services\StudentFinanceService::class)->totals(app(\App\Services\StudentFinanceService::class)->installments($student)))<p>الحالة المالية: {{ \App\Services\StudentFinanceService::STATUSES[$financial['status']] }}</p><p>الرقم الدراسي: {{ $student->student_number }}</p><p>الصف: {{ $student->classroom?->name ?? 'غير مسند' }}</p><p>البريد: {{ $student->user->email }}</p><p>أولياء الأمور: {{ $student->guardians->pluck('user.name')->join('، ') ?: 'غير مرتبط' }}</p></div>@endsection
