@extends('admin.layout') @section('title','الإعلانات') @section('actions')<a class="btn primary" href="{{ route('admin.announcements.create') }}">إعلان جديد</a>@endsection @section('content')<div class="table-wrap"><table><thead><tr><th>العنوان</th><th>الجمهور</th><th>النشر</th><th>الانتهاء</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($announcements as $a)<tr><td>{{ $a->title }}</td><td>{{ $a->audience }}</td><td>{{ $a->published_at?->format('Y-m-d H:i') ?? '—' }}</td><td>{{ $a->expires_at?->format('Y-m-d H:i') ?? '—' }}</td><td><span class="badge {{ $a->status }}">{{ $a->status }}</span></td><td><a href="{{ route('admin.announcements.edit',$a) }}">تعديل</a></td></tr>@empty<tr><td colspan="6"><div class="empty">لا توجد إعلانات.</div></td></tr>@endforelse</tbody></table></div>{{ $announcements->links() }}@endsection
