@extends('admin.layout') @section('title',$announcement->exists?'تعديل الإعلان':'إنشاء إعلان') @section('content')<form class="form-card" method="post" action="{{ $announcement->exists?route('admin.announcements.update',$announcement):route('admin.announcements.store') }}">@csrf @if($announcement->exists)@method('put')@endif<div class="form-grid"><label class="wide">العنوان<input name="title" value="{{ old('title',$announcement->title) }}" required></label><label class="wide">المحتوى<textarea rows="6" name="content" required>{{ old('content',$announcement->content) }}</textarea></label><label>الجمهور<select name="audience"><option value="all">الجميع</option><option value="students">الطلاب</option><option value="teachers">المعلمون</option><option value="parents">أولياء الأمور</option><option value="classroom">صف محدد</option></select></label><label>الصف<select name="classroom_id"><option value="">—</option>@foreach($classrooms as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></label><label>تاريخ النشر<input type="datetime-local" name="published_at" value="{{ old('published_at',$announcement->published_at?->format('Y-m-d\TH:i')) }}"></label><label>تاريخ الانتهاء<input type="datetime-local" name="expires_at" value="{{ old('expires_at',$announcement->expires_at?->format('Y-m-d\TH:i')) }}"></label><label>الحالة<select name="status"><option value="draft">مسودة</option><option value="published">منشور</option><option value="archived">مؤرشف</option></select></label></div><div class="form-actions"><button class="btn primary">حفظ</button></div></form>@endsection
