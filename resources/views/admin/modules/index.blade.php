@extends('admin.layout') @php($labels=['schedules'=>'الجداول','attendance'=>'الحضور','exams'=>'الاختبارات','grades'=>'الدرجات','library'=>'المكتبة','reports'=>'التقارير','users'=>'المستخدمون','roles'=>'الأدوار والصلاحيات','audit-logs'=>'سجل العمليات','settings'=>'الإعدادات','notifications'=>'الإشعارات']) @section('title',$labels[$module]) @section('content')@if(method_exists($data,'count') && $data->count())<div class="table-wrap"><table><thead><tr>@foreach(array_keys((array)$data->first()) as $k)<th>{{ $k }}</th>@endforeach</tr></thead><tbody>@foreach($data as $row)<tr>@foreach((array)$row as $value)<td>{{ is_scalar($value)?$value:'—' }}</td>@endforeach</tr>@endforeach</tbody></table></div>@if(method_exists($data,'links')){{ $data->links() }}@endif @else<div class="panel empty">لا توجد بيانات في قسم {{ $labels[$module] }} حاليًا.</div>@endif @endsection
