@extends('admin.layout') @section('title','تفاصيل المادة') @section('actions')<a class="btn primary" href="{{ route('admin.subjects.edit',$subject) }}">تعديل</a>@endsection @section('content')<div class="panel"><h2>{{ $subject->name }}</h2><p>الرمز: {{ $subject->code }}</p><p>{{ $subject->description }}</p><p>الصفوف: {{ $subject->classrooms->pluck('name')->join('، ') ?: 'لا توجد' }}</p></div>@endsection
