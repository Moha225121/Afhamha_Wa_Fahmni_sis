@extends('admin.layout') @section('title','المواد') @section('actions')<a class="btn primary" href="{{ route('admin.subjects.create') }}">إضافة مادة</a>@endsection @section('content')<form class="filters"><input name="q" value="{{ request('q') }}" placeholder="اسم المادة أو الرمز"><button class="btn secondary">بحث</button></form><div class="table-wrap"><table><thead><tr><th>المادة</th><th>الرمز</th><th>المرحلة</th><th>الصفوف</th><th>المعلمون</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($subjects as $s)<tr><td>{{ $s->name }}</td><td>{{ $s->code }}</td><td>{{ $s->stage }}</td><td>{{ $s->classrooms_count }}</td><td>{{ $s->teachers_count }}</td><td><span class="badge {{ $s->status }}">{{ $s->status }}</span></td><td><a href="{{ route('admin.subjects.edit',$s) }}">تعديل</a></td></tr>@empty<tr><td colspan="7"><div class="empty">لا توجد مواد حاليًا.</div></td></tr>@endforelse</tbody></table></div>{{ $subjects->links() }}@endsection
