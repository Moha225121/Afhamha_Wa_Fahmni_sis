@extends('admin.layout') @section('title','لوحة تحكم الإدارة') @section('subtitle','مؤشرات المدرسة المباشرة من قاعدة البيانات') @section('content')<section class="cards">@foreach(['students'=>'الطلاب','teachers'=>'المعلمون','parents'=>'أولياء الأمور','classes'=>'الصفوف','attendance_rate'=>'حضور اليوم %','absent'=>'الغياب اليوم','upcoming_exams'=>'الاختبارات القادمة','announcements'=>'الإعلانات النشطة'] as $key=>$label)<article class="card stat"><span>{{ $label }}</span><strong>{{ $stats[$key] }}</strong></article>@endforeach</section><section class="grid-2"><div class="panel"><h2>مؤشرات تشغيلية</h2><p class="muted">تظهر الإحصاءات بمجرد تسجيل الحضور والدرجات والاختبارات في النظام.</p><div class="cards"><div class="stat"><span>نسبة حضور اليوم</span><strong>{{ $stats['attendance_rate'] }}%</strong></div><div class="stat"><span>الغياب</span><strong>{{ $stats['absent'] }}</strong></div></div></div><div class="panel"><h2>آخر الأنشطة</h2>@forelse($activities as $a)<div class="activity">{{ $a->action }} — {{ $a->module }}<small>{{ $a->created_at?->diffForHumans() }}</small></div>@empty<div class="empty">لا توجد أنشطة مسجلة بعد.</div>@endforelse</div></section>@endsection
