@extends('admin.layout') @section('title','لوحة تحكم الإدارة') @section('subtitle','مؤشرات المدرسة المباشرة من قاعدة البيانات') @section('content')<section class="cards">@foreach(['students'=>'الطلاب','teachers'=>'المعلمون','parents'=>'أولياء الأمور','classes'=>'الصفوف','attendance_rate'=>'حضور اليوم %','absent'=>'الغياب اليوم','upcoming_exams'=>'الاختبارات القادمة','announcements'=>'الإعلانات النشطة'] as $key=>$label)<article class="card stat"><span>{{ $label }}</span><strong>{{ $stats[$key] }}</strong></article>@endforeach</section><section class="grid-2"><div class="panel"><h2>الحضور حسب السنة الدراسية</h2><div class="table-wrap"><table><thead><tr><th>السنة</th><th>الطلاب</th><th>الحضور</th><th>الغياب</th><th>التأخر</th><th>النسبة</th></tr></thead><tbody>@forelse($attendanceByYear as $year)<tr><td>{{ $year->name }}</td><td>{{ $year->total }}</td><td>{{ $year->present }}</td><td>{{ $year->absent }}</td><td>{{ $year->late }}</td><td><span class="badge active">{{ $year->rate }}%</span></td></tr>@empty<tr><td colspan="6"><div class="empty">لا توجد بيانات حضور.</div></td></tr>@endforelse</tbody></table></div></div><div class="panel"><h2>آخر الأنشطة</h2>@forelse($activities as $a)<div class="activity">{{ $a->action }} — {{ $a->module }}<small>{{ $a->created_at?->diffForHumans() }}</small></div>@empty<div class="empty">لا توجد أنشطة مسجلة بعد.</div>@endforelse</div></section>@endsection
