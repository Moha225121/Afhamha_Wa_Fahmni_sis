@extends('teacher.layout') @section('title','لوحة تحكم المعلم') @section('subtitle','ملخص مهامك التعليمية') @section('content')<section class="teacher-welcome"><div><h2>مرحبًا، {{ $teacher->user?->name ?? auth()->user()->name }}</h2><p>تابع الدروس والاختبارات والواجبات من مكان واحد.</p></div><a class="btn secondary" href="{{ route('teacher.lessons.create') }}">إنشاء درس جديد</a></section><section class="cards teacher-stats"><article class="card stat"><span>الطلاب</span><strong>{{ $stats['students'] }}</strong></article><article class="card stat"><span>اختبارات مسودة</span><strong>{{ $stats['draft_exams'] }}</strong></article><article class="card stat"><span>الدروس</span><strong>{{ $stats['lessons'] }}</strong></article></section><section class="grid-2"><div class="panel"><h2>الاختبارات القادمة</h2>@forelse($upcomingExams as $e)<div class="activity">{{ $e->title }} — {{ $e->subject }}</div>@empty<div class="empty">لا توجد اختبارات قادمة.</div>@endforelse</div><div class="panel"><h2>واجبات قادمة</h2>@forelse($upcomingAssignments as $a)<div class="activity">{{ $a->title }} — {{ $a->subject }}</div>@empty<div class="empty">لا توجد واجبات قادمة.</div>@endforelse</div></section>@endsection
