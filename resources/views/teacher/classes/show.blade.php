@extends('teacher.layout') @section('title',$classroom->name) @section('subtitle','الطلاب والعمليات الأكاديمية للصف') @section('content')<section class="cards"><article class="card stat"><span>عدد الطلاب</span><strong>{{ $classroom->students_count }}</strong></article><article class="card stat"><span>متوسط الصف</span><strong>{{ $averageGrade !== null ? number_format($averageGrade,1).'%' : '-' }}</strong></article></section><nav class="class-tabs"><a class="active" href="{{ route('teacher.classes.show',$classroom) }}">الطلاب</a><a href="{{ route('teacher.grades.index',['classroom_id'=>$classroom->id]) }}">الدرجات</a><a href="{{ route('teacher.assignments.index',['classroom_id'=>$classroom->id]) }}">الواجبات</a></nav><section class="panel"><div class="table-wrap"><table><thead><tr><th>#</th><th>الطالب</th><th>الرقم الدراسي</th><th>المعدل</th><th></th></tr></thead><tbody>@forelse($classroom->students as $student)<tr><td>{{ $loop->iteration }}</td><td>{{ $student->user?->name }}</td><td>{{ $student->student_number }}</td><td>{{ $student->average_grade !== null ? number_format($student->average_grade,1).'%' : '-' }}</td><td><a href="{{ route('teacher.students.show',$student) }}">عرض الملف</a></td></tr>@empty<tr><td colspan="5">لا يوجد طلاب.</td></tr>@endforelse</tbody></table></div></section>@endsection
